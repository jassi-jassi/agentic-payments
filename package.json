{
  "name": "agentic-payments",
  "version": "1.0.0",
  "description": "Multi-agent payments on Avalanche using x402 and ERC-8004",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:fuji": "hardhat run contracts/scripts/deploy.js --network fuji",
    "deploy:mainnet": "hardhat run contracts/scripts/deploy.js --network avalanche"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0",
    "dotenv": "^16.3.1"
  }
}
